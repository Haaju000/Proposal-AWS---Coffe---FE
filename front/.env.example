# Frontend Environment Variables (Optional)
# Note: Frontend auto-detects environment (localhost vs production)

# Backend API URLs are configured in src/config/environment.js

# AWS Cognito Configuration (if needed)
# REACT_APP_AWS_REGION=ap-southeast-1
# REACT_APP_COGNITO_USER_POOL_ID=your-user-pool-id
# REACT_APP_COGNITO_CLIENT_ID=your-client-id

# For manual environment override (if needed)
# REACT_APP_FORCE_ENV=PRODUCTION
